% Bar Chart

%x = (1:5);
%y = [1, 2, 3, 4, 5];
%bar(x,y), xlabel('x'), ylabel('y');



% Contour
% [x, y] = meshgrid(-4:0.1:4, -4:0.1:4);
% g = x.^2 + y.^2;
% contour(x, y, g);

% PS: draw a circle
% alpha = (0:pi/50:2*pi);
% R = 2;
% x = R*cos(alpha);
% y = R*sin(alpha);
% plot(x,y), axis([-2, 2, -2, 2]), grid on;

% Three dimensional (3-D) Plot
%[x, y] = meshgrid(-2: .2: 2);
%g = x .* exp(-x.^2 - y.^2);
%surf(x, y, g);






